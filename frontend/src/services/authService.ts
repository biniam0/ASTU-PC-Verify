// Authentication API calls
// e.g. login, logout, refreshToken, getCurrentUser
export const authService = {
  // login: (username, password) => apiRequest('/auth/login', { method: 'POST', body: JSON.stringify({ username, password }) }),
}
